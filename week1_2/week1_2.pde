background(255,0,25); //takes a color value gray(0 to 255) or rgb(0,0,0)
size(600,400);// 2 arguments: x, y dimensions
stroke(0,255,0);//color of the stroke around a shape
strokeWeight(10); //the thikness of the stroke in pixels
fill(0,0,255);//determines the color of a shape
rect(0,0,300,300); //4 arguments. creates a rectangle in pos x and y with size x and y

//another rectangle
stroke(0,0,200);//color of the stroke around a shape
strokeWeight(3); //the thikness of the stroke in pixels
noFill();//removes color from shape
rect(320,50,50,200); //4 arguments. creates a rectangle in pos x and y with size x and y
